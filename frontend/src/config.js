// Application version configuration — bump these when you release a new build.
// The app version shown on the About screen is assembled as:
//   <APP_MAJOR_VERSION>.<FRONTEND_VERSION>.<backend version from /api/version/>
// e.g. v1.f1.b1

export const APP_MAJOR_VERSION = 'v1' // application major version
export const FRONTEND_VERSION = 'f3' // frontend build version