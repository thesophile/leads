DB_NAME=leadsdb
DB_USER=root
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=3306

# SMTP (production): uncomment and fill in to send real password-reset emails.
# Leave these out to keep using the dev console backend (prints to terminal).
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=you@example.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=true
# EMAIL_USE_SSL=false
# DEFAULT_FROM_EMAIL=LEADS <you@example.com>